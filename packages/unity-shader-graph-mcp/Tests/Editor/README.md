# Editor Tests

This folder is reserved for Unity Editor tests for the Shader Graph MCP package.

Suggested first tests:

- request routing returns the expected action response
- null or empty asset paths are rejected where required
- response models serialize cleanly in the Unity test environment
- compatibility reports capture `unityVersion`, backend kind, and the resolved `GraphData` surface before package-backed mutations are trusted
- `create_graph` should regress the blank-only package-backed path
- `read_graph_summary`, `add_node`, `connect_ports`, and graph contract export/import should regress the current package-backed paths for `Vector1Node.Out -> Vector1Node.X`, `ColorNode.Out -> SplitNode.In`, `Vector4Node.Out -> SubGraphOutputNode.Out`, `UVNode.Out -> TilingAndOffsetNode.UV`, `TilingAndOffsetNode.Out -> SampleTexture2DNode.UV`, `Texture2DAssetNode.Out -> SampleTexture2DNode.Texture`, `Texture2DAssetNode.Out -> NormalFromTextureNode.Texture`, `UVNode/TilingAndOffsetNode.Out -> NormalFromTextureNode.UV`, `SampleTexture2DNode.RGBA -> SplitNode.In`, `SampleTexture2DNode.RGBA -> MultiplyNode.A`, `SampleTexture2DNode.RGBA -> NormalStrengthNode.In`, `SampleTexture2DNode.RGBA -> NormalUnpackNode.In`, `SampleTexture2DNode.RGBA -> NormalBlendNode.A/B`, `Vector2Node.Out -> NormalReconstructZNode.In`, `Vector1/arithmetic -> NormalStrengthNode.Strength`, `Vector1/arithmetic -> NormalFromTextureNode.Offset/Strength`, `NormalStrengthNode.Out -> SplitNode.In`, `SampleTexture2DNode.R/G/B/A -> Vector1Node.X`, `SplitNode.R/G/B/A -> Vector1Node.X`, `Vector1/Split/arithmetic -> CombineNode.R/G/B/A`, `Vector1/Split/arithmetic -> Vector2/Vector3/Vector4` scalar inputs, `CombineNode.RGBA -> SplitNode.In`, `Vector4Node.Out -> SplitNode.In`, the scalar arithmetic `Vector1Node.Out -> Add/Subtract/Multiply/Divide/Power/Minimum/Maximum/Modulo/Lerp/Smoothstep/Clamp/Step/Absolute/Floor/Ceiling/Round/Sign/Sine/Cosine/Tangent/Negate/Reciprocal/SquareRoot/Fraction/Truncate/Saturate/OneMinus` inputs, arithmetic `Out -> Vector1Node.X`, arithmetic `Out -> arithmetic` inputs, `Vector1/arithmetic -> Comparison.A/B`, `Comparison.Out -> one or more Branch.Predicate`, `Vector1/arithmetic -> Branch.True/False`, `Branch.Out -> one or more Vector1/arithmetic inputs`, `Color/Combine RGBA/Vector4/Multiply/Branch/Lerp/Append/SampleTexture2D.RGBA -> Multiply.A/B`, `Color/Combine RGBA/Vector4/Multiply/Branch/Lerp/Append/SampleTexture2D.RGBA -> Branch.True/False`, `Color/Combine RGBA/Vector4/Multiply/Branch/Lerp/Append/SampleTexture2D.RGBA -> Lerp.A/B/T`, `Color/Combine RGBA/Vector4/Multiply/Branch/Lerp/Append/SampleTexture2D.RGBA plus Vector1/Split/arithmetic/SampleTexture2D channels -> Append.A/B`, `Multiply.Out -> SplitNode.In`, `Branch.Out -> SplitNode.In`, `Lerp.Out -> SplitNode.In`, `AppendVectorNode.Out -> SplitNode.In`, `AppendVectorNode.Out -> SplitNode.In + MultiplyNode.A/B` fan-out, and the current sub graph contract round-trip that reuses the built-in `SubGraphOutputNode`
- `add_property` should still start with `Color` and `Float`/`Vector1` coverage
- `save_graph` should regress the package-backed validate + write + refresh path
- `supportedNodeTypes` should be treated as the current graph-addable subset, while `discoverableNodeTypes` can remain broader for diagnostics
- probe-rejected diagnostics should stay grouped by stable failure-reason buckets so catalog triage can distinguish reflection filters from runtime graph-creation, instantiation, layout, `AddNode`, and `ValidateGraph` failures
- preferred common graph-addable nodes such as `Add`, `Multiply`, `Lerp`, and `Clamp` should be exercised through asset-backed smoke tests whenever they remain present in the supported catalog
- connection-rule metadata should stay envelope-only so the runtime can publish `supportedConnectionRules` without changing the response shape, while ColorNode inputs remain unsupported and broader general fan-out beyond the current `Comparison`, `Branch`, and `Append` fan-out routes stays out of scope

Current smoke coverage:

- [`ShaderGraphPackageBackedSmokeTests.cs`](/Users/song/Projects/unity-shader-graph-mcp/packages/unity-shader-graph-mcp/Tests/Editor/ShaderGraphPackageBackedSmokeTests.cs) exercises the current real package-backed loop for blank `create_graph`, `read_graph_summary`, `add_property`, `add_node`, `save_graph`, blank subgraph lifecycle, the first subgraph composition path `Vector4 -> SubGraphOutput`, the current `connect_ports` paths across scalar, vector-builder, logic, early Color-routing coverage, the UV/texture-derived normal utility workflows through `NormalStrength`, `NormalUnpack`, `NormalBlend`, `NormalReconstructZ`, and `NormalFromTexture` inputs, explicit `Comparison` and `Branch` fan-out cases, append output fan-out into `Split` plus `Multiply`, mixed Append chaining into `Multiply`, `Lerp`, and `Branch`, the reverse mixed chains `Multiply/Lerp/Branch -> Append -> Split`, vector fan-in chains `Combine/Vector4 -> Append -> Split`, and fan-in continuation chains `Combine -> Append -> Lerp -> Split` plus `Vector4 -> Append -> Branch -> Split`.
- The same smoke suite now prefers repo-owned blank graph and blank subgraph fixtures created during the test run instead of copying package-cache Shader Graph samples.
- [`ShaderGraphFixtureSampleTests.cs`](/Users/song/Projects/unity-shader-graph-mcp/packages/unity-shader-graph-mcp/Tests/Editor/ShaderGraphFixtureSampleTests.cs) keeps repo-owned sample fixtures in `Tests/Fixtures` under regression coverage so the suite can read a stable graph and subgraph without depending on package cache assets.
- [`ShaderGraphTestAssets.cs`](/Users/song/Projects/unity-shader-graph-mcp/packages/unity-shader-graph-mcp/Tests/Editor/ShaderGraphTestAssets.cs) owns the temporary asset folder scope, package-ready guard, and small response readers used by the editor smoke tests.
