# 1.0.0 Final Checklist

Status: closed. `1.0.0` shipped and the remaining expansion work now rolls forward into [1.1.0-plan.md](/Users/song/Projects/unity-shader-graph-mcp/docs/1.1.0-plan.md).

## Goal

Promote the current package-backed engine to stable `1.0.0` without reopening broad engine scope.

## Definition Of Done

- The current package-backed editing engine remains stable on Unity 2022.3 with Shader Graph 17.3.0.
- Live MCP transport binding is finalized enough for real external tool calls through the Unity batchmode bridge.
- Release packaging and installation flow are documented from a fresh checkout.
- The verified runtime boundary is clearly documented so users understand what is and is not guaranteed.

## Must Finish Before 1.0.0

- Validate end-to-end external tool invocation against a real MCP client.
- Validate end-to-end external tool invocation against a real Unity Editor project through the batchmode bridge.
- Run a fresh-project install smoke test from the tagged release.
- Confirm the current verified node and connection matrix still passes in Unity EditMode tests.
- Confirm release docs and install docs reflect the actual shipped product shape.

Current ready-to-run checks:

- Unity release smoke menu: `Tools > Shader Graph MCP > Debug > Run Blank Graph Happy Path`
- Python live transport smoke test: `python3 -m unittest server.tests.test_mcp_transport_subprocess`
- Python live transport + fake Unity bridge smoke test: `python3 -m unittest server.tests.test_mcp_transport_subprocess`
- Real Unity bridge smoke helper: `python3.12 -m unity_shader_graph_mcp.real_mcp_smoke --asset-path "<graph path>" --timeout-seconds 180`
- Manual MCP payload guide: [mcp-smoke-payloads.md](/Users/song/Projects/unity-shader-graph-mcp/docs/mcp-smoke-payloads.md)

Execution split:

- Stream A: server transport and live MCP binding
- Stream B: Unity hardening and release-critical smoke coverage
- Stream C: contracts, install docs, happy-path docs, and release boundary freeze

See [1.0.0-work-split.md](/Users/song/Projects/unity-shader-graph-mcp/docs/1.0.0-work-split.md) for ownership and sequencing.

## Should Finish Before 1.0.0

- Clean up top-level setup instructions for Unity package import and server startup.
- Tighten error messages around unsupported node and port combinations.
- Freeze the supported matrix language in docs so the 1.0 boundary is explicit.
- Keep the install/startup guide in sync with the current blank-only and package-backed happy path: [install-and-happy-path.md](/Users/song/Projects/unity-shader-graph-mcp/docs/install-and-happy-path.md).
- Capture one recommended happy-path demo flow:
  - `create_graph`
  - `add_property`
  - `add_node`
  - `connect_ports`
  - `save_graph`

## Explicitly Not Required For 1.0.0

- Universal all-node support.
- Universal all-port connection support.
- Template-backed graph creation beyond the current blank-graph path.
- Broad expansion into every texture, UV, and custom-function workflow.

## Recommended Release Shape

- `1.0.0`
  - stable package-backed Shader Graph engine
  - live MCP transport binding
  - documented installation and verified runtime boundary
- `1.1.x`
  - broader node and connection matrix
  - texture and UV workflows
  - deeper authoring ergonomics

## Final Pre-Tag Pass

- Run Unity `EditMode` tests to full pass.
- Run `Tools > Shader Graph MCP > Debug > Run Blank Graph Happy Path`.
- Run `python3 -m unittest server.tests.test_mcp_transport_subprocess`.
- Verify package version and Python version metadata.
- Confirm [CHANGELOG.md](/Users/song/Projects/unity-shader-graph-mcp/CHANGELOG.md) includes all release-facing changes.
- Draft GitHub release notes before tagging.
